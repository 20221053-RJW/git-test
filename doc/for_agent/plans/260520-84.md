# [오늘 업무 계획] — 260520-84

> **계획 생성 시각:** 2026-05-20 18:11:36  
> **상태:** `done`  
> **날짜:** 2026-05-20  

---

## 오늘 목표 (한 줄)

다른 학생 프로필(`getStudentByIdFromDb`)을 ‘primary 수업’ 학생만 보는 오류에서, **접근 가능한 모든 수업** 중 한 곳이라도 학생 멤버십이 있으면 불러오도록 고친다.

---

## 수행 예정 작업

| 순서 | TODO | 내용 | 완료 기준 |
|------|------|------|-----------|
| 1 | T-083 | `supabase-api.ts` 멤버십 기반 학생 단건 조회 | 모달 외 경로(`/app/students/:id`)에서도 교차 수업 학생 표시 가능 |
| 2 | — | `OtherStudentProfilePage` 로딩/에러 처리 | 실패 시 로딩 해제 |

---

## 오늘 하지 않을 것 (스코프 밖)

- Q&A 페이지를 과목별 서브 라우트로 쪼개기(별도 기획)

---

## 승인

- [x] 사용자 「바로 작업 시작」(starter §0)

---

## 실행 결과

| 항목 | 결과 |
|------|------|
| 완료 | `getStudentByIdFromDb`가 `ai_course_memberships`(student)·접근 가능 `course_id`로 검증 후 `ai_users` 로드 |
| doc | `05`·`02`·`17`·`25_ai_work_log`·`current_session_plan` |

**인간에게:** 수강자 네트워크 등에서 상세 페이지로 들어간 학생은, 본인이 들을 수 있는 **어느 수업에서든** 같이 묶여 있으면 프로필이 열립니다.
