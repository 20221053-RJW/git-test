# [오늘 업무 계획] — 260519-1

> **계획 생성 시각:** 2026-05-19 (이관 — 원 `current_session_plan.md` 작성 시각 미보존)  
> **계획 완료 시각:** 2026-05-19 (세션 종료, E2E·CI 반영 완료)  
> **상태:** `done`  
> **날짜:** 2026-05-19  
> **세션:** T-040 Playwright · T-041 CI

---

## 오늘 목표 (한 줄)

핵심 사용자 플로우 5개 Playwright E2E와 PR용 GitHub Actions 워크플로를 추가한다.

---

## 수행 예정 작업

| 순서 | TODO | 내용 | 완료 기준 |
|------|------|------|-----------|
| 1 | T-040 | `playwright.config` + webServer + chromium | 로컬 dev 연동 |
| 2 | T-040 | `tests/e2e` 5플로우 + auth helper | 14_testing 시나리오 |
| 3 | T-040 | Navigation 로그아웃 버튼 | 플로우 5 |
| 4 | T-041 | `.github/workflows/e2e.yml` | PR 시 실행(시크릿 있을 때) |
| 5 | — | `.env.example` E2E 변수, package.json scripts | |
| 6 | doc | 02, 05, 14, 17 갱신 | |

---

## 오늘 하지 않을 것

- T-011 RLS Firebase (인간 리뷰)
- T-030 AI API

---

## 승인

- [x] 「더 진행해, 계획 제출하고 바로 시작해」

---

## 실행 결과

| 항목 | 결과 |
|------|------|
| T-040 | `playwright.config.ts`, `tests/e2e/core-flows.spec.ts`, `helpers/auth.ts`, Navigation `data-testid="logout-button"` |
| T-041 | `.github/workflows/e2e.yml` (PR/push, VITE_* + E2E 시크릿) |
| 빌드 | `npm run build` 성공 |
| E2E 로컬 | 인증 가드 1건 통과; 로그인 5건은 `.env`에 `E2E_TEST_*` 없으면 skip |
| doc 갱신 | `02`, `05`, `14`, `17`, `25_ai_work_log` |

**인간 후속:** `.env`에 `E2E_TEST_EMAIL` / `E2E_TEST_PASSWORD` 설정 후 `npm run test:e2e`. GitHub repo secrets 동일 키 + `VITE_*` 등록.
