# [오늘 업무 계획] — 260526-2

> **계획 생성 시각:** 2026-05-26 11:00:00  
> **상태:** `done`  
> **날짜:** 2026-05-26

---

## 오늘 목표 (한 줄)

채팅·쓰기 API 전송 지연을 줄이기 위해 세션 캐시·경량 수업 조회·peer 1회 검증·팀 sort_order SELECT 제거 (낙관적 UI 제외).

---

## 수행 예정 작업

| 순서 | 항목 | 내용 | 완료 기준 |
|------|------|------|-----------|
| 1 | 세션 캐시 | currentUser·accessibleCourseIds·courseStatus·teamCourseId·courseStudentIds | send 시 중복 조회 제거 |
| 2 | 경량 수업 | `getCourseStatusFromDb` 단일 SELECT | assertActive·팀 채팅 archived 검사 |
| 3 | 1:1 peer | `warmDirectChatSendContext` 스레드 오픈 시 1회 | send는 INSERT만 |
| 4 | 팀 채팅 | `warmTeamChatSendContext` + sort_order=Date.now() | send 전 MAX SELECT 제거 |
| 5 | doc | 25·current_session_plan | done 기록 |

---

## 오늘 하지 않을 것

- 낙관적 UI (사용자 요청 제외)

---

## 승인

- [x] 바로 착수

---

## 실행 결과

| 항목 | 결과 |
|------|------|
| 세션 캐시 | `api-session-cache.ts` · currentUser·accessible·status·teamCourse·studentIds |
| 경량 수업 | `getCourseStatusFromDb` — `assertActiveCourseMembership` 등 |
| 1:1 | `warmSendContext` · send는 warm 시 INSERT만 |
| 팀 채팅 | `warmChatSendContext` · `sort_order=Date.now()` |
| 로그아웃 | `invalidateApiSessionCache` |
| 빌드 | 통과 |
