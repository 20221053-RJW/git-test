# [오늘 업무 계획] — 260520-82

> **계획 생성 시각:** 2026-05-20 18:04:46  
> **상태:** `done`  
> **날짜:** 2026-05-20  

---

## 오늘 목표 (한 줄)

vision 추가요청 #13: 수업 스코프 수강자 네트워크에서 빈 DB를 데모 데이터로 채우던 UI 버그를 제거하고 관련 문서·스냅샷을 반영한다.

---

## 수행 예정 작업

| 순서 | TODO/항목 | 내용 | 완료 기준 | 비고 |
|------|-----------|------|-----------|------|
| 1 | vision #13 · T-081 | `StudentsNetworkPage`: `courseId`가 있으면 API 빈 배열을 그대로 사용, 데모 `fallbackStudents`/extras 미사용 | 신규 수업(학생 0)에서 가짜 카드 없음 | `getNetworkStudentsFromDb`는 이미 `course_id` 필터됨 |
| 2 | 버그픽스 | `useAuth`에서 `isAdmin` 구조 분해(랜덤팀 저장 권한 표현) | 참조 누락 없음 | |
| 3 | doc | `vision_snapshot.md`에 #13·#14 줄 동기화 · `27`·`02`·`05`·`17` · `for_human/26` 갱신 | ID·상태 일치 | |
| 4 | 종료 | 본 파일 실행 결과·상태 `done` · `current_session_plan.md` | 인덱스 반영 | |

---

## 오늘 하지 않을 것 (스코프 밖)

- RLS/H-001/H-002 등 인간 블로커 작업
- `RandomTeamPage`의 비–course 스코프 `api.students.getAll()` 전면 개편(별도 과제)

---

## 리스크·확인 필요 사항

- 수강자 0명일 때 교수·관리자에게는 빈 상태 화면만 보임(의도된 동작).

---

## 승인

- [x] 사용자 요청 「바로 작업 시작」(starter §0 3단계 생략)

---

## 실행 결과 (작업 종료 후 AI가 채움)

| 항목 | 결과 |
|------|------|
| 완료 | T-081: `StudentsNetworkPage` 수업 스코프에서 DB 빈 학생 목록을 데모로 대체하지 않음 + vision/doc 동기화 |
| 미완·보류 | `/app/teams/random`의 비–course `api.students.getAll()` 패턴(레거시)·H-001~H-007 인간 항목 |
| doc 갱신 | `02`, `05`, `17`, `27`, `vision_snapshot`, `for_human/01`, `for_human/26`, `25_ai_work_log`, `current_session_plan.md` |

**인간에게:** 새 수업에서 수강자 0명이면 «이 수업에 등록된 학생이 없습니다»만 보이며, 김학생 입장 후에는 해당 수업 `ai_course_memberships` 기준 목록만 갱신됩니다.
