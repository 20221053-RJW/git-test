# [오늘 업무 계획] — 260520-83

> **계획 생성 시각:** 2026-05-20 18:08:37  
> **상태:** `done`  
> **날짜:** 2026-05-20  

---

## 오늘 목표 (한 줄)

레거시 랜덤 팀 페이지를 수업 스코프·`api.students.getAll(courseId)`로 고정하고, `ProjectsPage`의 존재하지 않는 프로젝트 상세 경로 링크를 팀 워크스페이스로 교정한다.

---

## 수행 예정 작업

| 순서 | TODO/항목 | 내용 | 완료 기준 | 비고 |
|------|-----------|------|-----------|------|
| 1 | T-082-a | 라우트 `courses/:courseId/teams/random` 추가, `:teamId`보다 먼저 매칭 | `/teams/random` 접근 시 primary 수업으로 리다이렉트 | `CourseScopedRedirect`에 `random-teams` |
| 2 | T-082-b | `RandomTeamPage`가 `courseId`로 해당 수업 학생만 로드 | `getAll()` 무인자 호출 제거 | |
| 3 | T-082-c | `ProjectsPage` 카드 링크 → `/app/courses/{courseId}/teams/{teamId}` | 404 경로 제거 | |
| 4 | doc | `02`·`05`·`17`·`25_ai_work_log`·인덱스 | T-082 반영 | |

---

## 오늘 하지 않을 것 (스코프 밖)

- `ai_projects` 전용 상세 페이지 신규 구현(H-블로커 없음이면 추후)

---

## 승인

- [x] 사용자 「바로 작업 시작」(starter §0)

---

## 실행 결과

| 항목 | 결과 |
|------|------|
| 완료 | `CourseScopedRedirect` `random-teams`, `courses/:courseId/teams/random` 라우트(팀 상세보다 우선)·`ProjectsPage` 링크·`RandomTeamPage` 학생 로딩 스코프 |
| 미완·보류 | `ai_projects` 전용 페이지(선택 과제)·H 블로커 |
| doc 갱신 | `02`·`05`·`17`·`25_ai_work_log`·`current_session_plan` |

**인간에게:** 예전 책갈피 `/app/teams/random`은 자동으로 primary 수업의 `…/teams/random`으로 이동합니다. 프로젝트 카드는 팀 워크스페이스로 열립니다.